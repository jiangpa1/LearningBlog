# ══════════════════════════════════════════════════════════════════════
#  Docker 构建上下文排除清单
#  它决定「宿主机上哪些文件会被送进 docker build 的上下文」。
# ══════════════════════════════════════════════════════════════════════

# ★★★ 最重要的一行 ★★★
# application-local.yml 里有数据库 / Redis 的口令。它在 src/main/resources 下，
# 所以 `COPY src ./src` 会把它带进构建阶段 → 打进 jar → 进最终镜像。
# 镜像推到任何 registry，密码就跟着走。这是海南麻将那次「jar 里烤进配置」的翻版，只是换了载体。
src/main/resources/application-local.yml

# 构建产物：镜像里会重新 build，带进去只会拖慢上下文、还会让缓存失效
target/

# 版本控制与 IDE
.git/
.gitignore
.idea/
*.iml

# 本地环境变量文件（含口令）
.env

# 文档与本机脚本：镜像里用不到
md/
*.md
day*.ps1
